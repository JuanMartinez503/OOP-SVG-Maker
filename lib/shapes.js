class Shapes {
  //made a shapes class for the circle, square class to inherit
  constructor(name, textColor, fillColor) {
    this.name = name;
    this.textColor = textColor;
    this.fillColor = fillColor;
  }
  render() {
    "";
  }
}

module.exports = Shapes;
//this will export the class to be used in other files
